/**
 * Le type de picto qu'il peut y avoir sur une ligne d'item de JDD lié à un projet
 * LOGO = app-organization-logo
 * STATIC = ref vers un SVG
 */
export enum ProjectDatasetPictoType {
    LOGO, STATIC
}
