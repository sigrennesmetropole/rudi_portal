export class ObjectUtils {
    /**
     * Équivalent de la méthode Java Objets.nonNull
     * @param obj l'objet testé
     * @return true si l'objet est non nul
     */
    static nonNull(obj): boolean {
        return obj;
    }
}
