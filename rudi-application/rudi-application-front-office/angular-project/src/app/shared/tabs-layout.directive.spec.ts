import { TabsLayoutDirective } from './tabs-layout.directive';

describe('TabsLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
